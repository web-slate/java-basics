name: Generate Documentation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3  # Update to v3

    - name: Set up JDK 17
      uses: actions/setup-java@v3  # Keep v3 as it's already latest
      with:
        java-version: '17'
        distribution: 'adopt'
        
    - name: Cache Maven dependencies
      uses: actions/cache@v3  # Keep v3 as it's already latest
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-
          
    - name: Build with Maven
      run: mvn clean install
      
    - name: Generate Markdown Documentation
      run: |
        chmod +x ./generate_docs.sh  # Make the script executable
        ./generate_docs.sh            # Run the script to generate Markdown files
      
    - name: Generate JavaDoc
      run: mvn javadoc:javadoc
      
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4  # Update to v4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./target/site/apidocs
        keep_files: true
